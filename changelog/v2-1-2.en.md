# v2.1.2 — Template editor minor fixes

**Date:** July 31, 2026
**Type:** hotfix

### Fixed
- **Template file path:** the edit page now shows the actual file path instead of `{{ templatePath }}` (placeholder was in Twig syntax instead of Lang::t format)
- **Redundant label:** removed the «Content *» label above the editor — it duplicated the page title

### Added
- **File size** display on template editor and CSS editor pages — shows human-readable file size (B/KB/MB)

---

**Changed files:** `core/lang/ru.json`, `core/src/Admin/TemplateController.php`, `core/src/Admin/DesignController.php`, `core/views/admin/template/edit.html.twig`, `core/views/admin/design/css.html.twig`
