# Favorites v1.0.1 — V2 Compatibility Fix

**Дата:** 30 июля 2026
**Тип:** hotfix

**Исправления:**
- `init.php`: путь к конфигу обновлён с `front/config/config.php` на `config/front.php` + core defaults.
- `FavoritesController.php`: путь к конфигу исправлен (`../../../config/front.php` + core defaults).

---

# Favorites v1.0.1 — V2 Compatibility Fix

**Date:** July 30, 2026
**Type:** hotfix

**Fixes:**
- `init.php`: config path updated from `front/config/config.php` to `config/front.php` + core defaults.
- `FavoritesController.php`: config path fixed (`../../../config/front.php` + core defaults).
