# Account v1.0.1 — V2 Compatibility Fix

**Дата:** 30 июля 2026
**Тип:** hotfix

Удалена битая строка миграции `INSERT INTO plugin_account_users_migration`, которая вызывала ошибки при каждой загрузке плагина.

---

# Account v1.0.1 — V2 Compatibility Fix

**Date:** July 30, 2026
**Type:** hotfix

Removed broken migration line `INSERT INTO plugin_account_users_migration` that was causing errors on every plugin load.
